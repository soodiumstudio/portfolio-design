---
name: portfolio-design
description: 만들고 싶은 화면을 푸는 방법이 다른 세 가지 안으로 만들고, 기준을 세워 하나를 고르게 한다. 무엇을 포기했고 어떻게 메꿨는지까지 남긴다. 실행하면 준비물을 먼저 알려주고 확인받은 뒤 시작하며, 필요한 것은 선택지로 물어보고 진행률을 실시간으로 알려준다. 결과는 볼 수 있는 웹 페이지로 나온다. "화면 만들어줘", "UI 시안", "앱 화면 설계", "어떤 안으로 갈지", "화면 비교", "와이어프레임" 같은 요청에 사용한다.
---

# 화면 세 가지 만들고 하나 고르기

**하나를 잘 만드는 스킬이 아니다.** 푸는 방법이 다른 셋을 만들고, 기준을 세워 고르고,
무엇을 포기했는지 남기는 것이 결과물이다. 채점표에서 별표가 붙은 유일한 항목이 여기서 나온다.

## 물어보는 방식 — 학생은 고르기만 한다

학생은 **"해줘" 한 마디만 한다**고 가정한다. 자세히 설명해 달라고 하지 않는다.

1. **질문은 반드시 `AskUserQuestion` 도구로 한다.** 글로 나열하지 않는다.
   글로 물으면 학생이 한 줄로 뭉뚱그려 답하고, 그러면 안 물어본 것과 같다.
2. **선택지는 내가 만든다.** 받은 자료를 먼저 보고 그럴듯한 후보를 만든다.
   학생에게 백지에 서술하라고 시키지 않는다.
3. **질문은 최대 3번.** 그 안에 못 채운 것은 추측하고 "이렇게 가정했습니다"라고 알린다.
4. **모든 질문에 빠져나갈 선택지를 넣는다.** — "잘 모르겠어요" 또는 "알아서 해주세요".
   이게 없으면 학생은 아무것도 고르지 않고 창을 닫는다.
5. **선택지 설명에 결과를 적는다.** "이걸 고르면 무엇이 달라지는지"를 한 줄로.
   예) `직장인 견주 (설명: 산책 시간을 못 내는 문제로 좁혀집니다)`
6. **한 번에 한 주제만.** 성격이 다른 질문을 한 화면에 섞지 않는다.
7. **학생이 "몰라요", "알아서 해줘"라고 하면 더 묻지 않는다.** 바로 가정 모드로 간다.

**너무 자세히 묻지 않는다.** 학생이 답을 모르는 질문은 묻는 게 아니라 내가 정하고 확인만 받는다.

---

## 진행률 — 단계마다 반드시 먼저 출력한다

화면을 만드는 동안 몇 분씩 조용해진다. 그때 학생은 멈춘 줄 알고 창을 닫는다.
**단계가 바뀔 때마다 아래 한 줄을 먼저 출력한다.** 10칸 막대에 `▓` 와 `░` 를 쓴다.

```
▓▓▓▓▓░░░░░ 50%  |  안 B 만드는 중 (3개 중 2개째)
```

| 시점 | % | 붙이는 말 |
|---|---|---|
| 준비 확인 통과 | 10% | 화면에 뭐가 필요한지 정하는 중 |
| 화면 명세 확정 | 25% | 세 가지 접근 정하는 중 |
| 안 만들기 시작 | 35% | 안 A 만드는 중 (3개 중 1개째) |
| 안 두 개째·세 개째 | 50% / 65% | 안 B / 안 C 만드는 중 |
| 그림 저장 완료 | 70% | 비교 기준 고르는 중 |
| 비교 채점 끝 | 85% | 포기한 것 정리하는 중 |
| 정리 완료 | 95% | 볼 수 있게 만드는 중 |
| 아티팩트 발행 | 100% | 끝났습니다 |

**안을 만드는 구간(35~65%)이 제일 길다.** 하나 끝날 때마다 알린다.

## 시작 게이트 — 준비 확인부터 한다 (건너뛰지 않는다)

> ⛔ **학생이 "완료"를 고르기 전에는 아무것도 만들지 않기.**

**1) 아래 카드를 그대로 출력한다.**

```
📋 시작 전에 확인해 주세요

① 지금 열려 있는 폴더가 맞나요?
   화면 그림과 정리 파일이 이 폴더에 저장됩니다.

② 무엇을 만들고 싶은지 한 줄로 말할 수 있나요?
   "자취생이 저녁 메뉴 고르는 화면" 정도면 충분합니다.
   아직 막연해도 괜찮습니다. 제가 같이 좁혀 드립니다.

③ 45분 정도 시간이 있나요?
   화면 세 개를 만들고 그중 하나를 고르는 데까지 갑니다.

📎 있으면 좋은 것 (없어도 됩니다)
   문제 정의를 적어 둔 게 있으면 이 채팅창에 붙여넣어 주세요.
   훨씬 정확한 화면이 나옵니다.
```

**2) 그다음 `AskUserQuestion` 으로 확인받는다.**

```
header: "준비"
question: "준비되셨나요?"
options:
  - "네, 시작할게요"          (설명: 화면에 무엇이 필요한지 정하는 질문으로 넘어갑니다)
  - "아직 뭘 만들지 모르겠어요" (설명: 관심 있는 것부터 같이 좁혀 드릴게요)
  - "문제 정의를 붙여넣을게요"  (설명: 붙여넣으시면 그걸 읽고 시작하겠습니다)
```

---

## 1단계 — 화면에 무엇이 필요한지 정한다 (질문 3개, 전부 선택지)

바로 만들지 않는다. 여기를 건너뛰면 예쁘지만 문제와 상관없는 화면이 나오고,
채점에서 최대 1점이 된다.

학생이 문제 정의를 붙여넣었으면 그것을 읽고 **후보를 내가 만든다.**
없으면 학생이 말한 한 줄에서 만든다. 백지에 쓰라고 하지 않는다.

### 질문 1 (필수) — 이 화면에서 끝내는 일

```
header: "목표"
question: "이 화면에서 사용자가 무엇을 끝내면 되나요?"
options:
  - "오늘 저녁 메뉴를 정하기"     (설명: 고르고 나면 끝나는 흐름입니다)
  - "장을 봐서 결제까지 하기"     (설명: 결제 화면까지 만들게 됩니다)
  - "일주일 식단을 짜 두기"       (설명: 여러 날을 한 번에 다루는 화면이 됩니다)
  - "잘 모르겠어요"              (설명: 제가 가장 좁은 것으로 정하고 알려드릴게요)
```

### 질문 2 (필수) — 여기서 사용자가 골라야 하는 것

**이게 이 화면의 핵심이다.** 고를 게 두 개 이상이면 화면을 나눠야 한다는 신호다.

```
header: "결정"
question: "사용자가 이 화면에서 무엇을 골라야 하나요? 하나만요."
options:
  - "무엇을 먹을지"          (설명: 메뉴를 고르는 화면이 중심이 됩니다)
  - "언제 먹을지"            (설명: 시간·요일을 고르는 화면이 중심이 됩니다)
  - "얼마를 쓸지"            (설명: 예산이 중심이 됩니다)
  - "알아서 정해 주세요"      (설명: 목표에 맞는 것으로 제가 정하겠습니다)
```

### 질문 3 (필수) — 세 안을 어떻게 다르게 할까

**색이나 버튼 모양이 아니라 "사용자에게 무엇을 시키느냐"가 달라야 한다.**
학생이 이해하기 쉽게 조합을 통째로 제시한다.

```
header: "세 가지 방향"
question: "세 화면을 이렇게 다르게 만들어 볼까요?"
options:
  - "다 보여주기 / 하나만 추천 / 하나씩 묻기 (추천)"
      (설명: 가장 많이 쓰이는 조합입니다. 비교하기도 쉽습니다)
  - "빠르게 끝내기 / 꼼꼼히 고르기 / 남이 대신 골라주기"
      (설명: 속도와 정성 사이에서 갈리는 조합입니다)
  - "혼자 쓰기 / 같이 정하기 / 기록 남기기"
      (설명: 누구와 쓰는지가 갈리는 조합입니다)
  - "제가 정해 주세요"    (설명: 목표와 결정에 맞는 조합을 제가 만들겠습니다)
```

답을 **한 문단으로 정리해 읽어 주고 확인받는다.** 확인 전에는 만들지 않는다.
정리한 내용을 `screen-brief.md` 로 저장한다.

---

## 2단계 — 안 세 개를 만든다

`design` 스킬을 쓸 수 있으면 그것으로 만든다. 없으면 화면 구성을 글과 그림으로 설명한다.
**각 안마다 1단계의 목표·결정·필수 정보를 그대로 반영한다.**

하나가 끝날 때마다 진행률과 함께 **무엇이 달라졌는지 한 줄**로 알린다.
> `▓▓▓▓▓░░░░░ 50%` 안 B 완성 — A는 목록을 다 보여주고, B는 추천 하나만 보여줍니다.

**같은 화면에 색만 바꾼 세 개는 세 개가 아니다.** 그렇게 나왔으면 다시 만든다.

---

## 3단계 — 그림으로 저장한다

```
design/options/A/01.png, 02.png …
design/options/B/…
design/options/C/…
design/options/notes.md   ← 각 안이 어떤 접근인지 한 줄씩
```

**학생에게 내보내는 법을 시키지 않는다.** 내가 저장하고 어디 있는지만 알려 준다.

---

## 4단계 — 비교 기준을 학생이 고른다

기준을 내가 정하지 않는다. 다만 **후보는 내가 만든다.** 1단계 목표에서 뽑는다.

```
header: "비교 기준"
question: "세 안을 무엇으로 비교할까요? 두세 개 골라 주세요."
multiSelect: true
options:
  - "빨리 끝나는가"        (설명: 몇 번 눌러야 끝나는지로 비교합니다)
  - "고르기 쉬운가"        (설명: 처음 쓰는 사람이 헤매지 않는지로 비교합니다)
  - "내 마음에 맞는가"     (설명: 원하는 걸 정확히 찾을 수 있는지로 비교합니다)
  - "다시 쓰고 싶은가"     (설명: 두 번째 사용이 편한지로 비교합니다)
```

고른 기준으로 표를 만든다. **각 칸에 점수만 쓰지 말고 왜 그 점수인지 한 줄**을 붙인다.
점수만 있는 표는 나중에 질문받으면 무너진다.

---

## 5단계 — 고르고, 포기한 것을 적는다 ★ 여기가 결승선

고르는 것은 학생이다. 표를 보여 주고 `AskUserQuestion` 으로 묻는다.
선택지 설명에 **각 안을 고르면 무엇을 얻고 무엇을 잃는지**를 적는다.

고른 뒤 `tradeoff.md` 를 네 칸으로 만든다. 그대로 포트폴리오 한 페이지가 된다.

```markdown
# 왜 이 안을 골랐는가
## 1. 안 A·B·C        {각 안 그림 + 한 줄 설명}
## 2. 비교 기준        {기준과 그 기준을 고른 이유}
## 3. 선택과 이유      {고른 안 + 근거}
## 4. 포기한 것과 보완
- 포기한 것: {구체적으로}
- 그게 사용자에게 주는 불편: {한 문장}
- 그 불편을 어떻게 줄였는가: {구체적 설계}
```

**4번이 비면 끝난 게 아니다.** "포기한 게 없다"고 하면 다시 묻는다.
> 안 B를 골랐다는 건 안 A가 잘하던 무언가를 버렸다는 뜻입니다. 안 A만 할 수 있었던 게 뭐였죠?

---

## 결과는 아티팩트(볼 수 있는 웹 페이지)로 만든다

**학생은 `.md` 파일을 열지 않는다.** "파일을 확인하세요"로 끝내면 아무도 안 본다.

1. **채팅에 핵심을 먼저 쓴다.** 3~5줄. 아티팩트를 열지 않아도 결론은 알 수 있게.
2. **`Artifact` 도구로 페이지를 발행한다.**
   - 만들기 전에 `artifact-design` 스킬을 먼저 읽는다.
   - 제목은 학생의 프로젝트 이름으로. (예: `반려견 산책 앱 리서치`)
   - `favicon` 이모지 하나, `description` 한 줄.
3. **파일도 폴더에 저장한다.** 근거 보관용이지 학생이 읽을 것은 아니다.
4. **학생에게는 링크를 준다.** 그리고 "이 페이지를 그대로 포트폴리오에 붙이거나, 캡처해서 쓰셔도 됩니다"라고 알린다.

### 아티팩트에 담을 것 — 이 순서를 지킨다

| 순서 | 무엇 | 규칙 |
|---|---|---|
| ① 한눈에 보기 | 제일 중요한 것 3개 | 한 문장씩. 숫자가 있으면 크게 |
| ② 그래서 무슨 뜻인가요 | 해석 | 나열이 아니라 읽어 주기. 이 칸이 비면 실패다 |
| ③ 근거 | 표·출처·페이지 번호 | 접어 두어도 된다. 확인하고 싶은 사람만 편다 |
| ④ 다음에 할 일 | 3개까지 | 오늘 할 수 있는 크기로 쪼갠다 |

**어려운 말을 쓰지 않는다.** 중학생이 읽어도 걸리는 데가 없어야 한다.
전문 용어가 꼭 필요하면 처음 나올 때 괄호로 뜻을 적는다.

아티팩트에는 **세 안의 그림을 나란히 놓고**, 그 아래 비교표와 "포기한 것"을 넣는다.
학생이 이 페이지를 캡처해서 포트폴리오에 그대로 넣을 수 있어야 한다.

---

## 지키는 것

1. **모르면 후보를 만들어 고르게 한다** — 백지에 쓰라고 하지 않는다.
2. **근거를 붙인다** — 비교표의 모든 점수에 이유 한 줄.
3. **고르는 건 학생이다** — 특히 5단계. AI가 고르면 나중에 "왜 이렇게 했나요"에 답할 수 없다.
4. **길게 쓰지 않는다** — 안은 셋, 기준은 셋.
5. **쉬운 말로 말한다** — 어려운 말은 처음 나올 때 괄호로 뜻을 적는다.
6. **명령이나 설정을 시키지 않는다** — 파일 경로, 터미널 명령을 학생에게 시키지 않는다.

결과는 학생이 지금 열어 둔 폴더에 저장한다. 다른 스킬이나 이전 결과물을 필요로 하지 않는다.

## 끝낼 때

먼저 `▓▓▓▓▓▓▓▓▓▓ 100%  |  끝났습니다` 를 출력하고 아티팩트 링크를 준다. 그다음:

```
확인해 보세요
□ 세 안이 '푸는 방법'이 다른가요? 색만 다른 건 아닌가요?
□ '포기한 것'이 구체적으로 적혀 있나요? ('없음'은 오답입니다)
□ 이 페이지만 보고 남이 왜 그 안인지 이해할 수 있나요?
```

친구에게 이 페이지만 보여 주고 물어보라고 안내한다 — "내가 왜 이걸 골랐는지 알겠어?"
설명을 덧붙여야 알아듣는다면 아직 덜 된 것이다.
